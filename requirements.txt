numpy>=1.24
sounddevice>=0.4.6,<0.6
websockets>=13
python-dotenv>=1.0,<2
requests>=2.28
vosk>=0.3.44
pywebview>=5.0
openwakeword>=0.6
onnxruntime>=1.16
pyobjc-framework-ApplicationServices>=10

# la voix et l'oreille d'ava, toutes deux sur le gpu du mac (mlx). c'est le
# chemin par defaut : rien ne sort de la machine, rien ne s'epuise.
#   kokoro  82 M en 4 bits   « Oui ? » en 0,065 s, briefing de 31 s en 1,70 s
#   whisper large-v3-turbo   3,5 s de commande transcrits en 0,23 s
mlx-audio>=0.4
mlx-whisper>=0.4
# ⚠️ le g2p francais de kokoro passe par espeak : sans `espeak-ng` (brew) et ces
# deux paquets, kokoro parle anglais tres bien et se tait en francais.
misaki>=0.7
espeakng-loader>=0.2

# --- replis, plus jamais automatiques ---------------------------------------
# gardes parce qu'ils sont selectionnables dans les reglages, mais plus personne
# n'y retombe tout seul : un seul chemin, et `say` comme filet.
faster-whisper>=1.0        # si la machine n'a pas d'apple silicon
elevenlabs>=1.50,<3
